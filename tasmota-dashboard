#nginx Site Configuration for Tasmota Dashboard with CORS support

server {
    listen 8080; # Der Port, über den dein Dashboard die Geräte anspricht
    server_name localhost;

    # Diese Sektion erlaubt CORS für alle Anfragen
    location / {
        # Erlaube Anfragen von allen Quellen (für das lokale LAN)
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;
        add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;

        # Behandle Vorab-Anfragen (Preflight) von Browsern
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
            add_header 'Access-Control-Max-Age' 1728000;
            add_header 'Content-Type' 'text/plain; charset=utf-8';
            add_header 'Content-Length' 0;
            return 204;
        }

        location ~ ^/tasmota/(?<target_ip>[\d\.]+)/(.*)$ {
            add_header 'Access-Control-Allow-Origin' '*' always;
            proxy_pass http://$target_ip/$2$is_args$args;

            proxy_http_version 1.1;
            #proxy_set_header Connection "";
        }
    }
}
